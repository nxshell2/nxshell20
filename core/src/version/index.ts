import versionData from './version.json';

export const version: string = versionData.version;
export const portable: boolean = versionData.portable;
export const weblink: string = versionData.weblink;
